# ────────────────────────────────────────────────────────────────────────────
# Traffic Congestion Pattern Discovery & Visualization — Python requirements
# Tested on Python 3.9 – 3.12. Python 3.13+ also works but requires the
# numpy 2.x / scikit-learn 1.4+ combination below (no prebuilt wheels exist
# for numpy 1.x on Python 3.13+).
# ────────────────────────────────────────────────────────────────────────────

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.23.0,<3.0.0
pyarrow>=14.0.0

# Machine learning
scikit-learn>=1.4.0
joblib>=1.2.0
kneed>=0.8.0          # principled elbow detection (optional — graceful fallback if missing)

# Web application
streamlit>=1.25.0
pydeck>=0.8.0
folium>=0.14.0
streamlit-folium>=0.13.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.15.0

# Geospatial (for scripts/make_centroids.py)
geopandas>=0.13.0
shapely>=2.0.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0
requests>=2.31.0

# Development / tests
pytest>=7.4.0
pytest-cov>=4.1.0
